\section{AdaGlimpse: Active Visual Exploration with Arbitrary Glimpse Position and Scale}
\label{sec:adaglimpse}

\subsection{Motivation}
\label{sec:motivation-adaglimpse}

Beyond Grids (Section~\ref{sec:beyond}) showed that a ViT can be made elastic enough to accept patches of any position and scale without significant accuracy loss, and that adaptive non-uniform sampling provides a measurable performance advantage at low patch budgets even with simple heuristic sampling strategies. The next step is to replace these hand-designed heuristics with a learned policy that, at each exploration step, observes the partial information accumulated so far and selects the position and scale of the next glimpse so as to maximise task performance.

As reviewed in Section~\ref{sec:adaptive-scope}, prior AVE methods, whether they pick glimpses directly from a full-scene grid or refine a low-resolution overview with fixed-scale crops, are constrained to discrete, grid-aligned action spaces and cannot exploit variable-scale observations even on hardware that natively supports zoom. AdaGlimpse bridges this gap by formulating glimpse selection over a fully continuous three-dimensional action space of position and scale, and training a Soft Actor-Critic agent~\cite{haarnoja2018soft} to navigate it.

It thereby investigates Hypothesis~\ref{hyp:adaglimpse}: an agent can learn to select both the position and scale of each observation in a continuous space, discovering an efficient coarse-to-fine exploration strategy that reduces the number of pixels it needs to observe.

\subsection{Adaptive glimpse sampling}
\label{sec:adaglimpse-formulation}

\textbf{Glimpse parameterisation.} Let $X$ be the scene to be explored, represented as a rectangle. A glimpse $G$ is a square region of $X$ captured by a camera with a fixed sensor resolution of $d_{\mathrm{cam}} \times d_{\mathrm{cam}}$ pixels. A glimpse is specified by its top-left corner $(x, y)$ and its \emph{scale} $z = (d - d_{\mathrm{min}}) / (d_{\mathrm{max}} - d_{\mathrm{min}})$, where $d$ is the physical size of the glimpse in the scene, and $d_{\mathrm{min}}$, $d_{\mathrm{max}}$ are the minimum and maximum camera field of view. Intuitively, scale $z = 0$ corresponds to maximum camera zoom (smallest, highest-detail glimpse) and $z = 1$ to the widest possible view (full-scene overview at lowest detail level). Glimpse coordinates $C = (x, y, z) \in [0, 1]^3$ form the continuous action space.

\textbf{AVE as a Markov Decision Process (MDP).} The exploration process unfolds over at most $T$ steps. At step $t$, the agent captures glimpse $G_t$ at coordinates $C_t$ selected by the policy, yielding a $d_{\mathrm{cam}} \times d_{\mathrm{cam}}$ image patch (simulated by cropping from a high-resolution image and rescaling). The agent maintains a growing sequence of observed patches $G_1, \ldots, G_t$ and their coordinates $C_1, \ldots, C_t$. Exploration terminates when either the maximum step count is reached or a task-specific confidence threshold is exceeded (early stopping). Figure~\ref{fig:adaglimpse-teaser} illustrates the resulting coarse-to-fine exploration behaviour.

\begin{figure}[th]
    \centering
    \includegraphics[width=0.9\linewidth]{papers_src/AdaGlimpse/figures/rlglimpse.pdf}
    \caption[AdaGlimpse: coarse-to-fine glimpse selection]{\textbf{AdaGlimpse in action.} The agent first captures a wide, low-resolution view of the entire scene (scale~$\approx 1$). Since no confident prediction can be made from this overview alone, it selects a second, smaller glimpse at a higher zoom level focusing on the most informative region. This coarse-to-fine process continues until a class probability exceeds a confidence threshold. Reproduced from~\pubcite{pardyl2024adaglimpse}.}
    \label{fig:adaglimpse-teaser}
\end{figure}

\subsection{AdaGlimpse}
\label{sec:adaglimpse-arch}

AdaGlimpse consists of two interacting components: a vision transformer backbone that processes the accumulated glimpses and produces both task predictions and representations for the RL agent, and a Soft Actor-Critic policy that selects the next glimpse coordinates. The overall design is shown in Figure~\ref{fig:adaglimpse-arch}.

\begin{figure}[th]
    \centering
    \includegraphics[width=\linewidth]{papers_src/AdaGlimpse/figures/rlarch.pdf}
    \caption[AdaGlimpse architecture overview]{\textbf{Architecture.} AdaGlimpse combines an ElasticViT encoder with a task-specific decoder (top) and a Soft Actor-Critic RL agent (bottom). At each step, the RL agent selects the position and scale of the next glimpse from the encoder's output, their coordinates, importance scores, and latent representations. Reproduced from~\pubcite{pardyl2024adaglimpse}.}
    \label{fig:adaglimpse-arch}
\end{figure}

\textbf{Glimpse encoder.} The encoder is a ViT-B (12 transformer blocks and embedding size 768) with the ElasticViT positional encoding from Section~\ref{sec:beyond}: each patch is positioned using the four-dimensional continuous sine-cosine encoding of its corner coordinates (Equation~\ref{eq:pos-enc}). At step $t$, the encoder receives all patches from glimpses $G_1, \ldots, G_{t-1}$ and their coordinates, concatenated into sequences $\widehat{G}_t$ and $\widehat{C}_t$. Additionally, the encoder computes an attention-rollout importance score for each patch~\cite{abnar2020quantifying}, forming a sequence $\widehat{I}_t$. The transformer blocks produce a sequence of latent tokens $\widehat{H}_t$ (one per patch) plus a class token.

\textbf{Task-specific decoder.} For classification, a linear layer acting on the class token suffices. For dense prediction tasks (reconstruction and segmentation) a MAE-style decoder of 8 transformer blocks (embedding size 512) is used. Unlike standard MAE, which inserts mask tokens only for the unobserved image regions, AdaGlimpse inserts mask tokens for \emph{all} grid positions in the output. This is necessary because variable-scale glimpses can overlap, meaning no fixed partition of ``seen'' and ``unseen'' regions exists. Any output pixel may have been observed by multiple glimpses at different resolutions, and the decoder must reconcile these into a coherent full-image prediction.

\textbf{Training objectives.} The backbone is optimised on the task loss computed at the \emph{final} step $t = T$ only (not at intermediate steps). For reconstruction the loss is RMSE, for classification and segmentation, knowledge distillation against a teacher model (DeiT ViT for classification and DeepLabV3/ResNet-101 for segmentation) is used via KL divergence. This matches the training setup of STAM~\cite{rangrej2022consistency} and enables a fair comparison.

\begin{figure}[th]
    \centering
    \includegraphics[width=0.75\linewidth]{papers_src/AdaGlimpse/figures/rlrl.pdf}
    \caption[AdaGlimpse RL agent: actor and critic]{\textbf{RL agent.} The actor selects the next glimpse position and scale $(x, y, z)$ from state $s_t = (\widehat{G}_t, \widehat{C}_t, \widehat{I}_t, \widehat{H}_t)$, the critic estimates the expected cumulative reward $Q(s_t, a_t)$ for that action. Actor and critic share the same input encoding architecture but have separate parameters. Reproduced from~\pubcite{pardyl2024adaglimpse}.}
    \label{fig:adaglimpse-rl}
\end{figure}

\textbf{State, action, and reward.} The RL agent's state at step $t$ is the tuple $s_t = (\widehat{G}_t,\, \widehat{C}_t,\, \widehat{I}_t,\, \widehat{H}_t)$: the raw patches, their coordinates, their importance scores, and their backbone latent representations. The action is $a_t = (x, y, z) \in [0, 1]^3$, the continuous coordinates of the next glimpse. The reward is defined as the improvement in the task loss: $r_t = L_{t-1} - L_t$, where $L_t$ is the task loss at step $t$. This direct signal encourages the agent to select glimpses that maximally reduce the residual task error.

\textbf{RL agent.} The agent uses Soft Actor-Critic with separate actor and critic networks. Each component of the state receives its own encoder: a small CNN for the raw patches $\widehat{G}_t$, and small MLPs for the coordinate, importance, and latent sequences $\widehat{C}_t$, $\widehat{I}_t$, $\widehat{H}_t$. The four resulting per-patch embeddings are concatenated and aggregated across patches via an attention-pooling layer~\cite{ilse2018attention}. The resulting fixed-size vector is passed through a final MLP to produce either the action distribution (actor) or the $Q$-value estimate (critic). The maximum-entropy SAC objective (Equation~\ref{eq:sac-objective}) prevents premature convergence to a deterministic greedy policy and encourages the agent to explore diverse glimpse sequences during training. Figure~\ref{fig:adaglimpse-rl} shows the RL module.

\textbf{Training schedule.} Pre-training consists of 600 epochs with 196 random glimpses per image sampled from a uniform position-scale distribution. Then, the model is trained for 100 epochs with early stopping: for the first 30 epochs, only the RL agent is updated (allowing it to learn an initial policy), while from epoch 31 onward, backbone and RL agent are optimised in alternating epochs.

\subsection{Experimental results}
\label{sec:adaglimpse-results}

\begin{table}[th]
    \caption[Reconstruction results across datasets and pixel budgets]{\textbf{Reconstruction results.} RMSE (lower is better) for AdaGlimpse against baselines across four datasets at three pixel budgets. Pixel~\% denotes the fraction of scene pixels seen by the model. $\dag$: reproduced result not published in the relevant paper. *: zero-shot performance (trained on ImageNet-1k only). AdaGlimpse outperforms all baselines at every pixel budget. Adapted from~\pubcite{pardyl2024adaglimpse}.}
    \label{tab:adaglimpse-reconstruction}
    \begin{center}
    \begin{small}
    \begin{tabular}{l@{\hskip6pt}lllllr@{\hskip8pt}cr}
    \toprule
    Method & IMNET & SUN360 & ADE20k & COCO & Image res. & Glimpses & Regime & Pixel \% \\
    \midrule
    AME & 30.3$^\dag$ & 29.8 & 30.8 & 32.5 & $128 \times 256$ & $8 \times 32^2$ & simple & 25.00 \\
    \textbf{AdaGlimpse (ours)} & \textbf{14.5} & \textbf{11.1}* & \textbf{14.0}* & \textbf{14.5}* & $224 \times 224$ & $12 \times 32^2$ & adaptive & 24.49 \\
    \midrule
    AttSeg & -- & 37.6 & 36.6 & 41.8 & $128 \times 256$ & $8 \times 48^2$ & retinal & 18.75 \\
    GlAtEx & -- & 33.8 & 41.9 & 40.3 & $128 \times 256$ & $8 \times 48^2$ & retinal & 18.75 \\
    AME & -- & 23.6 & 23.8 & 25.2 & $128 \times 256$ & $8 \times 48^2$ & retinal & 18.75 \\
    SimGlim & -- & 26.2 & 27.2 & 29.8 & $224 \times 224$ & $37 \times 16^2$ & simple & 18.75 \\
    AME & -- & 23.4 & 26.2 & 28.6 & $224 \times 224$ & $37 \times 16^2$ & simple & 18.75 \\
    \textbf{AdaGlimpse (ours)} & \textbf{14.7} & \textbf{11.1}* & \textbf{14.2}* & \textbf{14.7}* & $224 \times 224$ & $9 \times 32^2$ & adaptive & 18.36 \\
    \midrule
    AME & -- & 37.9 & 40.7 & 43.2 & $128 \times 256$ & $8 \times 16^2$ & simple & 6.25 \\
    \textbf{AdaGlimpse (ours)} & \textbf{20.9} & \textbf{17.6}* & \textbf{20.5}* & \textbf{21.5}* & $224 \times 224$ & $12 \times 16^2$ & adaptive & 6.12 \\
    \bottomrule
    \end{tabular}
    \end{small}
    \end{center}
\end{table}

\textbf{Reconstruction.} Table~\ref{tab:adaglimpse-reconstruction} compares AdaGlimpse with AME~\pubcite{pardyl2023active}, AttSeg~\cite{seifi2020segment}, GlAtEx~\cite{seifi2021glimpse}, and SimGlim~\cite{Jha2023WACV} on image reconstruction (RMSE, lower is better) across four datasets (ImageNet-1k, SUN360~\cite{xiao2012recognizing}, ADE20K~\cite{zhou2017scene}, and MS COCO~\cite{lin2014microsoft}) at three pixel budgets. A notable property of AdaGlimpse is that it is trained on ImageNet-1k only. The SUN360, ADE20K, and COCO results represent zero-shot transfer without any per-dataset fine-tuning.

At the $\approx$6\% pixel budget, AdaGlimpse achieves a lower RMSE (17.6--21.5 across datasets) than AME does at $\approx$18\% pixels (23.4--28.6). The coarse-to-fine strategy allows the model to first establish a global scene context with a single wide glimpse and then concentrate remaining observations on the most informative regions, which is impossible with a fixed-scale policy.

\begin{table}[th]
    \caption[Classification results on ImageNet-1k]{\textbf{Classification results.} Top-1 accuracy on ImageNet-1k. AdaGlimpse achieves the best accuracy at the same pixel budget as STAM (28.57\%), and with early stopping at 85\% confidence, reaches STAM-level accuracy using $\sim$40\% fewer pixels. Adapted from~\pubcite{pardyl2024adaglimpse}.}
    \label{tab:adaglimpse-classification}
    \begin{center}
    \begin{small}
    \begin{tabular}{l@{\hskip12pt}l@{\hskip12pt}r@{\hskip12pt}c@{\hskip12pt}r}
    \toprule
    Method & Accuracy \% & Glimpses & Regime & Pixel \% \\
    \midrule
    DRAM & 67.50 & $8 \times 77^2$ & full+simple & 94.53 \\
    GFNet & 75.93 & $5 \times 96^2$ & full+simple & 91.84 \\
    Saccader & 70.31 & $6 \times 77^2$ & full+simple & 70.90 \\
    TNet & 74.62 & $6 \times 77^2$ & full+simple & 70.90 \\
    STN & 71.40 & $9 \times 56^2$ & full+simple & 56.25 \\
    PatchDrop & 76.00 & $\sim8.9 \times 56^2$ & full+simple+stopping & $\sim$55.63 \\
    STAM & 76.13 & $14 \times 32^2$ & simple & 28.57 \\
    \textbf{AdaGlimpse (ours)} & \textbf{77.54} & $14 \times 32^2$ & adaptive & 28.57 \\
    \textbf{AdaGlimpse (ours)} & \textbf{76.30} & $\sim8.3 \times 32^2$ & adaptive+stopping & $\sim$\textbf{16.94} \\
    \bottomrule
    \end{tabular}
    \end{small}
    \end{center}
    \end{table}
    \begin{figure}[th]
\begin{center}
\begin{small}
\begin{subfigure}[b]{.48\textwidth}
\begin{tikzpicture}
\tikzstyle{every node}=[font=\scriptsize]
\begin{axis}[
    title={SUN360 reconstruction},
    xlabel={Pixel \%},
    ylabel={Root-mean-square error},
    xtick={0,5,10,15,20,25},
    xmin=0,
    xmax=25,
    ymajorgrids=true,
    grid style=dashed,
    scale only axis,
    height=3cm,
    ymax=85,
    width=.8\linewidth,
    legend cell align={left},
    legend style={font=\tiny}
]
\addplot[color=crimson2143940]
    coordinates {
    (2.34,68.99)(4.68,62.32)(7.03,56.24)(9.375,49.57)(11.71,46.09)(14.06,41.49)(16.40,37.49)(18.75,35.71)(21.09,34.66)(23.43,31.58)
    };
\addlegendentry{AttSeg}
\addplot[color=forestgreen4416044]
    coordinates {
    (2.34,70.50)(4.68,59.81)(7.03,51.77)(9.375,46.25)(11.71,42.45)(14.06,40.31)(16.40,37.16)(18.75,34.72)(21.09,32.00)(23.43,31.58)
    };
\addlegendentry{GlAtEx}
\addplot[color=darkorange25512714]
    coordinates {
    (2.34,46.89)(4.68,40.11)(7.03,35.40)(9.375,31.99)(11.71,28.99)(14.06,26.93)(16.40,25.23)(18.75,23.69)(21.09,22.49)(23.43,21.56)
    };
\addlegendentry{AME}
\addplot[color=steelblue31119180, very thick]
    coordinates {
    (2.04,25.17)(4.08,19.73)(6.12,17.21)(8.16,14.45)(10.20,12.58)(12.24,11.92)(14.28,11.54)(16.32,11.34)(18.36,11.22)(20.40,11.16)(22.44,11.11)(24.48,11.12)
    };
\addlegendentry{AdaGlimpse (ours)}
\end{axis}
\end{tikzpicture}
\end{subfigure}
\hfill
\begin{subfigure}[b]{.48\textwidth}
\begin{tikzpicture}
\tikzstyle{every node}=[font=\scriptsize]
\begin{axis}[
    title={ImageNet-1k classification},
    xlabel={Pixel \%},
    ylabel={Accuracy \%},
    xtick={0,5,10,15,20,25,30},
    ymajorgrids=true,
    grid style=dashed,
    scale only axis,
    height=3cm,
    legend pos=south east,
    width=.8\linewidth,
    legend cell align={left},
    legend style={font=\tiny}
]
\addplot[color=red]
    coordinates {
(2.04,14.52)(4.08,35.64)(6.12,47.64)(8.16,55.56)(10.20,60.91)(12.24,64.68)(14.28,67.47)(16.32,69.66)(18.36,71.25)(20.40,72.60)(22.44,73.73)(24.48,74.64)(26.53,75.47)(28.57,76.13)
    };
\addlegendentry{STAM}
\addplot[color=steelblue31119180, very thick]
coordinates {
(2.04,29.57)(4.08,49.69)(6.12,58.64)(8.16,64.04)(10.20,67.89)(12.24,70.76)(14.28,72.61)(16.32,74.12)(18.36,75.27)(20.40,75.94)(22.44,76.54)(24.48,76.96)(26.53,77.44)(28.57,77.54)
};
\addlegendentry{AdaGlimpse (ours)}
\addplot[color=mediumpurple148103189]
coordinates {
(9.87,67.21)(10.89,69.66)(12.06,71.62)(13.26,73.64)(14.87,74.94)(16.93,76.30)(21.18,77.15)(28.14,77.45)
};
\addlegendentry{AdaGlimpse (ours) (stopping)}
\end{axis}
\end{tikzpicture}
\end{subfigure}
\end{small}
\end{center}
\vspace{-.2cm}
\caption[Pixel efficiency curves for AdaGlimpse vs.\ baselines]{\textbf{Pixel \% vs.\ performance.} \emph{Left}: SUN360 reconstruction RMSE (lower is better) as a function of the fraction of pixels observed. AdaGlimpse surpasses all baselines at any pixel budget and achieves better reconstruction with 5\% of pixels than AME achieves with 25\%. \emph{Right}: ImageNet-1k classification accuracy. AdaGlimpse exceeds STAM at every budget, and the early-stopping variant matches STAM's accuracy using $\sim$40\% fewer pixels. Adapted from~\pubcite{pardyl2024adaglimpse}.}
\label{fig:adaglimpse-nglimpses}
\end{figure}

\begin{figure}[th]
    \begin{center}
    \begin{small}
    \begin{tabular}{r@{\hskip2pt}c@{\hskip2pt}c@{\hskip2pt}c@{\hskip2pt}c@{\hskip2pt}c@{\hskip2pt}|@{\hskip2pt}c@{\hskip2pt}c}
    A) & \tabfigure{width=\smallerimg}{papers_src/AdaGlimpse/figures/cls_im/4/601_1_0.png} & \tabfigure{width=\smallerimg}{papers_src/AdaGlimpse/figures/cls_im/4/601_2_0.png} & \tabfigure{width=\smallerimg}{papers_src/AdaGlimpse/figures/cls_im/4/601_3_0.png} & \tabfigure{width=\smallerimg}{papers_src/AdaGlimpse/figures/cls_im/4/601_4_0.png} & \tabfigure{width=\smallerimg}{papers_src/AdaGlimpse/figures/cls_im/4/601_5_0.png} & \tabfigure{width=\smallerimg}{papers_src/AdaGlimpse/figures/cls_im/2/140_1_0.png} & \tabfigure{width=\smallerimg}{papers_src/AdaGlimpse/figures/cls_im/2/140_2_0.png} \\[19pt]
    B) & \tabfigure{width=\smallerimg}{papers_src/AdaGlimpse/figures/cls_im/4/601_1_1.png} & \tabfigure{width=\smallerimg}{papers_src/AdaGlimpse/figures/cls_im/4/601_2_1.png} & \tabfigure{width=\smallerimg}{papers_src/AdaGlimpse/figures/cls_im/4/601_3_1.png} & \tabfigure{width=\smallerimg}{papers_src/AdaGlimpse/figures/cls_im/4/601_4_1.png} & \tabfigure{width=\smallerimg}{papers_src/AdaGlimpse/figures/cls_im/4/601_5_1.png} & \tabfigure{width=\smallerimg}{papers_src/AdaGlimpse/figures/cls_im/2/140_1_1.png} & \tabfigure{width=\smallerimg}{papers_src/AdaGlimpse/figures/cls_im/2/140_2_1.png} \\[19pt]
    C) & snail & hopper & agama & hopper & {\color{forestgreen4416044}mantis} & weevil & {\color{forestgreen4416044}bee eater} \\[2pt]
    D) & 22\% & 33\% & 49\% & 36\% & {\color{forestgreen4416044}76\%} & 26\% & {\color{forestgreen4416044}88\%}
    \end{tabular}
    \end{small}
    \end{center}
    \vspace{-.1cm}
    \caption[Glimpse selection step-by-step for ImageNet classification]{\textbf{Glimpse selection step-by-step.} AdaGlimpse explores $224\times224$ ImageNet images using $32\times32$ glimpses of variable scale, stopping once the predicted probability exceeds 75\% (a lower threshold than the 85\% used for the quantitative results in Table~\ref{tab:adaglimpse-classification}, chosen here to keep the illustrated episodes short). The vertical bar separates two independent examples. Row A: glimpse locations overlaid on the original image. Row B: pixels visible to the model (interpolated for display). Row C: current predicted label. Row D: prediction confidence (green = correct and above threshold). Reproduced from~\pubcite{pardyl2024adaglimpse}.}
    \label{fig:adaglimpse-qualitative}
    \end{figure}


\textbf{Classification.} Table~\ref{tab:adaglimpse-classification} shows classification results on ImageNet-1k against a broad set of baselines including methods that take a low-resolution overview plus fixed-scale glimpses (DRAM~\cite{ba2015multiple}, GFNet~\cite{wang2020glance}, Saccader~\cite{elsayed2019saccader}, TNet~\cite{papadopoulos2021hard}, STN~\cite{recasens2018learning}, and PatchDrop~\cite{uzkent2020learning}) and the state-of-the-art grid-based method STAM~\cite{rangrej2022consistency}.

\textbf{Pixel efficiency curves.} Figure~\ref{fig:adaglimpse-nglimpses} shows the relationship between the fraction of image pixels observed and task performance, sweeping over the number of glimpses. AdaGlimpse consistently outperforms baselines across the full pixel-budget spectrum. In reconstruction, it achieves lower error with 5\% of pixels than competing methods achieve with 25\%.


\textbf{Qualitative glimpse sequences.} Figure~\ref{fig:adaglimpse-qualitative} shows two representative classification sequences. The policy takes an initial wide-field glimpse, then progressively zooms in on discriminative regions, and halts as soon as the confidence threshold is exceeded, illustrating the coarse-to-fine strategy emerging from the learned policy without any explicit inductive bias towards it.

Further experimental results are presented in the original paper~\pubcite{pardyl2024adaglimpse}.

\subsection{Contributions}
\label{sec:contributions-adaglimpse}

The publication ``AdaGlimpse: Active Visual Exploration with Arbitrary Glimpse Position and Scale''~\pubcite{pardyl2024adaglimpse} makes the following contributions to the field of active visual exploration:

\begin{itemize}
    \item It introduces AdaGlimpse, a novel AVE method that selects glimpses in a fully continuous three-dimensional position-scale action space, overcoming the grid constraint shared by prior approaches. AdaGlimpse is able to learn the coarse-to-fine exploration without any explicit inductive bias towards it.
    \item It formulates AVE as a Markov Decision Process with a four-component state space $(\widehat{G}_t, \widehat{C}_t, \widehat{I}_t, \widehat{H}_t)$ and trains it with the Soft Actor-Critic algorithm, whose maximum-entropy objective encourages diverse, exploratory policies. The policy operates on a single task-agnostic transformer backbone, in which only the decoder head changes between reconstruction, classification, and segmentation.
    \item It demonstrates state-of-the-art performance across three visual tasks and four datasets. In reconstruction, AdaGlimpse achieves better results with 6\% of pixels than competing methods achieve with 18\%. In classification, it surpasses the best prior method (STAM) in absolute accuracy and can match STAM's accuracy using 40\% fewer pixels with early stopping. In segmentation, it matches AME while observing 35\% fewer pixels.
\end{itemize}

The learned coarse-to-fine behaviour and the improvements in pixel efficiency across reconstruction, classification, and segmentation confirm Hypothesis~\ref{hyp:adaglimpse} within the evaluated settings.

I was the primary author of this paper, and contributed to the conception of the idea, as well as majority of the experimental design and implementation. Furthermore, I conducted more than half of the experiments presented in the paper. I have presented this research at the European Conference on Computer Vision -- ECCV 2024, held in Milan, Italy. The code, data and further visualisations are available on my website at \url{https://io.pardyl.com/AdaGlimpse/}.
